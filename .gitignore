# --- Python ---
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/
.eggs/
*.egg

# --- Virtual environments ---
.venv/
venv/
env/
ENV/

# --- Test / coverage / type / lint caches ---
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
.coverage.*
coverage.xml
htmlcov/
.tox/

# --- Local databases (never commit) ---
*.db
*.sqlite
*.sqlite3
*.db-journal
data/*.db
data/*.sqlite3

# --- Generated mission artifacts (never commit) ---
/artifacts/*
!/artifacts/.gitkeep

# --- Source cache (raw external payloads; never commit) ---
/cache/*
!/cache/.gitkeep

# --- Reference source documents (binary; preserved locally, not tracked) ---
# Narrow rule: only the original .docx vision documents. Their checksums are in
# docs/reference/REFERENCE_MANIFEST.md and readable derivatives are tracked under
# docs/reference/extracted/. Remove this rule to track the originals (owner decision).
docs/reference/*.docx
docs/reference/*.pdf

# --- Secrets / local environment (never commit) ---
.env
.env.*
!.env.example
*.pem
*.key
secrets/

# --- IDE / OS ---
.idea/
.vscode/
*.swp
.DS_Store
Thumbs.db

# --- Tooling ---
.python-version
node_modules/
