# Normalize line endings to LF in the repository (prevents CRLF churn on Windows).
* text=auto eol=lf

# Binary artifacts (none tracked today, but keep them binary if added).
*.png binary
*.db binary
