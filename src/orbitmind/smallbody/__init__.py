"""Small-body (asteroid/comet) intelligence from JPL SSD/CNEOS (Phase 3A).

Small bodies use heliocentric orbital models — never SGP4. All values are
source-reported unless explicitly labelled as an OrbitMind calculation; missing
data is ``None`` (never zero).
"""
