# Alembic configuration for OrbitMind.
# The database URL is injected programmatically from application settings in
# migrations/env.py (ORBITMIND_DATABASE_URL), so it is intentionally not set here.

[alembic]
script_location = migrations
prepend_sys_path = src
path_separator = os
